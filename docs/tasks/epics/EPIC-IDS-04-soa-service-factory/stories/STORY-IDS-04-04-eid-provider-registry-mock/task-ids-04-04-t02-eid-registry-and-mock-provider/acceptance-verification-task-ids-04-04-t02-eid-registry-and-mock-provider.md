# Acceptance verification — task-ids-04-04-t02-eid-registry-and-mock-provider

- **Gate:** PASS (2026-05-30)
- **Wave:** pkg-000005
- **Evidence:** `EIDProviderRegistry` typed on `EIDProviderPort`; `MockEIDProvider` with `/auth/mock/callback`; no `@lru_cache`
